# This file specifies the direct dependencies of the application.
# Pip will resolve the correct versions of the underlying libraries.

Flask-SocketIO==5.3.6
websockets==12.0
janus==1.0.0
python-dotenv==1.0.1
requests==2.32.3
aiohttp==3.9.5
